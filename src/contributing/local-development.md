---
title: 本地开发
prev: false
next: false
---

# 本地开发

项目使用 pnpm 和 VitePress。

```sh
pnpm install
pnpm dev
```

提交前运行：

```sh
pnpm check
pnpm build
```

`check` 依次检查格式、TypeScript 和主题代码；`build` 还会验证学习图内容并检查内部链接。
